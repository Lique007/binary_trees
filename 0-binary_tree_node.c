#include <stdlib.h>
#include "binary_trees.h"

/* Create a node */

binary_tree_t *binary_tree_node(binary_tree_t *parent, int value);
int main(void)
	
struct *node createNode(value) {
struct *binary_tree_node  newNode = malloc(sizeof(struct node));
	newNode -> item = value;
	newNode -> left = NULL;
	newNode -> right = NULL;

	return newNode;
}

